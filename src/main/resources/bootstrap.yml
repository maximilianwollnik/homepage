spring:
  application:
    name: homepage
  cloud:
    config:
      uri: ${CONFIGPROTOCOL}://${CONFIGUSER}:${CONFIGPASSWORD}@${CONFIGURI}:${CONFIGPORT}

logging:
    file: /root/logs/application.log
